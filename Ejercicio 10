{
    List<string> nombres = new List<string>();
    void Start()
    {

        nombres.Add("juan");
        nombres.Add("maliketh");
        nombres.Add("pedrito");
        nombres.Add("malina");

        Debug.Log(nombres.Count);

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
