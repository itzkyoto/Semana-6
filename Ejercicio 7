{
    string[] nombres = new string[3] {"juan", "maliketh", "pepe" };

    void Start()
    {
        nombres[1] = "astolfo";

        Debug.Log(nombres[1]);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
